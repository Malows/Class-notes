export enum StoreKey {
  FACULTIES = "faculties",
  SUBJECTS = "subjects",
  PERIODS = "periods",
  COMMISSIONS = "commissions",
  ASSIGNMENTS = "assignments",
  STUDENTS = "students",
  NAV = "nav",
}
