## 1. Store Refactoring

- [x] 1.1 Encapsulate `subjectService` calls in `subjectsStore`.
- [x] 1.2 Encapsulate `periodService` calls in `periodsStore`.
- [x] 1.3 Encapsulate `commissionService` calls in `commissionsStore`.
- [x] 1.4 Encapsulate `studentService` calls in `studentsStore`.
- [x] 1.5 Encapsulate `assignmentService` calls in `assignmentsStore`.
- [x] 1.6 Encapsulate `correctionService` calls in `correctionsStore`.

## 2. Component Refactoring

- [x] 2.1 Update all pages/components to stop importing services and use stores exclusively.
