# Project Instructions

## Conventions

- **Package Manager**: Always use `pnpm` instead of `npm` or `yarn`.
- **Architecture**: Frontend follows a Clean Architecture pattern with Services, Stores, and decoupled Components.
