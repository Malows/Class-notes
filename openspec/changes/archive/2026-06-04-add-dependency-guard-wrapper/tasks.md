## 1. Core Implementation

- [x] 1.1 Create `src/lib/components/GuardWrapper.svelte`.

## 2. Refactoring

- [x] 2.1 Refactor `subjects/+page.svelte` to use `GuardWrapper` checking `facultiesStore`.
- [x] 2.2 Refactor `periods/+page.svelte` (or equivalent) to use `GuardWrapper` checking `subjectsStore`.
